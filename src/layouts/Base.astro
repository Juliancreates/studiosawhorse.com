---
interface Props {
  title?: string;
  fullBleed?: boolean;
  bodyScroll?: boolean;
}

const { title = 'Julian Emsley', fullBleed = false, bodyScroll = false } = Astro.props;
const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
  if (path === '/') return currentPath === '/' || currentPath.startsWith('/furniture');
  return currentPath.startsWith(path);
};
---

<html lang="en" class={bodyScroll ? 'body-scroll' : ''}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400&display=swap" rel="stylesheet">
    <title>{title}</title>
  </head>
  <body>

    <!-- Mobile: persistent top nav bar -->
    <nav class="mobile-nav-bar">
      <div class="mobile-menu">
        <button class="mobile-menu-label" id="mobile-menu-btn" aria-expanded="false">Menu</button>
        <div class="mobile-dropdown" id="mobile-dropdown" aria-hidden="true">
          <a href="/" class={`mobile-drop-link${isActive('/') ? ' active' : ''}`}>Furniture</a>
          <a href="/about" class={`mobile-drop-link${isActive('/about') ? ' active' : ''}`}>About</a>
        </div>
      </div>
      <a href="/" class="nav-logo">
        <img src="/images/logo.png" alt="Sawhorse" />
      </a>
      <div class="mobile-nav-right">
        <a href="https://instagram.com/studiosawhorse" target="_blank" rel="noopener" class="nav-link">@studiosawhorse</a>
      </div>
    </nav>

    <!-- Desktop: top nav bar -->
    <header class="topbar">
      <div class="topbar-menu">
        <span class="menu-label">Menu</span>
        <nav class="dropdown">
          <a href="/" class={isActive('/') ? 'active' : ''}>Furniture</a>
          <a href="/about" class={isActive('/about') ? 'active' : ''}>About</a>
        </nav>
      </div>
      <a href="/" class="logo">
        <img src="/images/logo.png" alt="Sawhorse" />
      </a>
      <div class="topbar-right">
        <a href="https://instagram.com/studiosawhorse" target="_blank" rel="noopener" class="topbar-ig">@studiosawhorse</a>
      </div>
    </header>

    <main class={fullBleed ? 'full-bleed' : ''}>
      <slot />
    </main>

  </body>
</html>

<script>
  const btn      = document.getElementById('mobile-menu-btn');
  const dropdown = document.getElementById('mobile-dropdown');

  btn?.addEventListener('click', () => {
    const isOpen = dropdown?.classList.contains('open');
    dropdown?.classList.toggle('open', !isOpen);
    btn.classList.toggle('open', !isOpen);
    btn.setAttribute('aria-expanded', String(!isOpen));
    dropdown?.setAttribute('aria-hidden', String(isOpen));
  });

  // Close when clicking outside
  document.addEventListener('click', (e) => {
    if (!btn?.contains(e.target as Node) && !dropdown?.contains(e.target as Node)) {
      dropdown?.classList.remove('open');
      btn?.classList.remove('open');
      btn?.setAttribute('aria-expanded', 'false');
      dropdown?.setAttribute('aria-hidden', 'true');
    }
  });
</script>

<style is:global>
  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    font-family: system-ui, sans-serif;
    background: #fff;
    color: #222;
  }

  /* ── Logo ── */
  .logo { display: block; }
  .logo img { width: auto; display: block; }

  /* ── Shared ── */
  .section-label {
    font-size: 0.7rem;
    letter-spacing: 0.12em;
    color: #aaa;
    text-transform: uppercase;
  }

  /* ═══════════════════════════════
     MOBILE
  ═══════════════════════════════ */

  @media (max-width: 767px) {
    /* Lock viewport — no page scroll */
    html, body { height: 100svh; overflow: hidden; }
    body { display: flex; flex-direction: column; }

    /* Nav bar: always visible, sits above content in flex stack */
    .mobile-nav-bar {
      flex-shrink: 0;
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      background: #fff;
      padding: 0.75rem 1.25rem;
      z-index: 20;
    }

    .nav-logo img {
      height: 70px;
      width: auto;
      display: block;
    }

    .mobile-menu { position: relative; }

    .mobile-menu-label {
      background: none;
      border: none;
      padding: 0;
      cursor: pointer;
      font-family: 'Lato', sans-serif;
      font-size: 14px;
      color: #999;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      transition: color 0.2s ease;
    }
    .mobile-menu-label.open { color: #000; }

    .mobile-dropdown {
      display: flex;
      flex-direction: column;
      position: absolute;
      top: calc(100% + 0.5rem);
      left: 0;
      background: #fff;
      padding: 0.75rem 1.25rem;
      min-width: 120px;
      opacity: 0;
      pointer-events: none;
      transform: translateY(-4px);
      transition: opacity 0.2s ease, transform 0.2s ease;
      z-index: 30;
    }
    .mobile-dropdown.open {
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }

    .mobile-drop-link {
      font-family: 'Lato', sans-serif;
      font-size: 14px;
      color: #999;
      text-decoration: none;
      padding: 0.4rem 0;
      transition: color 0.2s ease;
    }
    .mobile-drop-link.active { color: #dca26d; }
    .mobile-drop-link:hover:not(.active) { color: #000; }

    .mobile-nav-right { display: flex; justify-content: flex-end; }

    .nav-link {
      font-family: 'Lato', sans-serif;
      font-size: 13px;
      color: #999;
      text-decoration: none;
      transition: color 0.2s ease;
    }
    .nav-link:hover { color: #000; }
    .nav-link.active { color: #dca26d; }

    /* Main: flex child fills remaining viewport height */
    main {
      flex: 1;
      min-height: 0;
      overflow: hidden;
      padding: 0;
    }
    main:not(.full-bleed) {
      padding: 1.5rem;
      overflow-y: auto;
    }
    main.full-bleed {
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    /* Desktop topbar hidden on mobile */
    .topbar { display: none; }
  }

  /* About page (and any bodyScroll page): allow normal document scroll so Safari hides its UI on scroll */
  @media (max-width: 767px) {
    html.body-scroll,
    html.body-scroll body {
      height: auto;
      overflow: visible;
    }
    html.body-scroll .mobile-nav-bar {
      position: sticky;
      top: 0;
      z-index: 20;
    }
    html.body-scroll main,
    html.body-scroll main:not(.full-bleed) {
      flex: none;
      overflow: visible;
      padding: 0;
    }
  }

  /* Hide mobile elements at desktop widths */
  @media (min-width: 768px) {
    .mobile-nav-bar { display: none; }
  }

  /* ═══════════════════════════════
     DESKTOP
  ═══════════════════════════════ */
  @media (min-width: 768px) {
    /* Top nav bar: fixed across the top */
    .topbar {
      display: grid;
      grid-template-columns: 1fr auto 1fr;
      align-items: center;
      position: fixed;
      top: 0; left: 0; right: 0;
      height: 60px;
      padding: 0 2.5rem;
      background: #fff;
      z-index: 10;
    }

    .topbar .logo { justify-self: center; }
    .topbar .logo img { height: 80px; }

    .topbar-menu {
      position: relative;
      display: inline-block;
    }

    .menu-label {
      font-family: 'Lato', sans-serif;
      font-size: 14px;
      color: #999;
      cursor: default;
      letter-spacing: 0.05em;
      text-transform: uppercase;
      transition: color 0.2s ease;
    }

    .topbar-menu:hover .menu-label { color: #000; }

    .dropdown {
      display: flex;
      flex-direction: column;
      gap: 0;
      position: absolute;
      top: 100%;
      left: 0;
      background: #fff;
      padding: 0.75rem 1.25rem;
      min-width: 120px;
      opacity: 0;
      pointer-events: none;
      transform: translateY(-4px);
      transition: opacity 0.2s ease, transform 0.2s ease;
    }

    .topbar-menu:hover .dropdown {
      opacity: 1;
      pointer-events: auto;
      transform: translateY(0);
    }

    .dropdown a {
      text-decoration: none;
      color: #999;
      font-family: 'Lato', sans-serif;
      font-size: 14px;
      padding: 0.4rem 0;
      transition: color 0.2s ease;
    }
    .dropdown a.active { color: #dca26d; }
    .dropdown a:hover:not(.active) { color: #000; }

    .topbar-right { display: flex; justify-content: flex-end; }
    .topbar-ig { color: #999; text-decoration: none; font-family: 'Lato', sans-serif; font-size: 13px; transition: color 0.2s ease; }
    .topbar-ig:hover { color: #000; }

    /* Regular pages: push content below topbar */
    main {
      margin-left: 0;
      padding: 2.5rem;
      padding-top: calc(60px + 2.5rem);
      min-height: 100vh;
      overflow-y: auto;
    }

    /* Gallery pages: fill full viewport */
    main.full-bleed {
      padding: 0;
      padding-top: 60px;
      position: fixed;
      inset: 0;
      overflow: hidden;
    }
  }

</style>
