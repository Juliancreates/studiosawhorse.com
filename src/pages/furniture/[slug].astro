---
import Base from '../../layouts/Base.astro';
import { furniture } from '../../data/furniture';

export function getStaticPaths() {
  return furniture.map(p => ({ params: { slug: p.slug }, props: { piece: p } }));
}

const { piece } = Astro.props;
---

<Base title={`${piece.title} | Sawhorse`} fullBleed={true}>
  <div class="detail">
    <div class="detail-images">
      {piece.images.map(img => (
        <img
          src={img.src.src}
          alt={img.alt}
          loading="eager"
          decoding="async"
          class="detail-img"
        />
      ))}
    </div>
    <div class="detail-info">
      <p class="info-title">{piece.title}</p>
      <p class="info-year">{piece.year}</p>
      <p class="info-medium">{piece.medium}</p>
      <p class="info-dims">{piece.dimensions}</p>
      {piece.price && <p class="info-price">{piece.price}</p>}
      {piece.buyLink && <a href={piece.buyLink} class="buy-btn" target="_blank" rel="noopener">BUY</a>}
      {piece.blurb && <p class="info-blurb">{piece.blurb}</p>}
      {piece.sold && <p class="info-sold">{piece.sold}</p>}
      <a href="/" class="back-link">← Furniture</a>
    </div>
  </div>
</Base>

<style>
  /* ── Mobile ── */
  @media (max-width: 767px) {
    .detail {
      display: flex;
      flex-direction: column;
      height: 100%;
      overflow: hidden;
    }

    .detail-images {
      flex-shrink: 0;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      overflow-x: auto;
      overflow-y: hidden;
      height: 60%;
      padding: 0.75rem 1rem;
      -webkit-overflow-scrolling: touch;
    }

    .detail-img {
      height: 100%;
      width: auto;
      object-fit: contain;
      flex-shrink: 0;
      display: block;
    }

    .detail-info {
      flex: 1;
      min-height: 0;
      overflow-y: auto;
      padding: 1.25rem 1rem 2rem;
      display: flex;
      flex-direction: column;
      gap: 0.35rem;
    }
  }

  /* ── Desktop ── */
  @media (min-width: 768px) {
    .detail {
      position: fixed;
      inset: 0;
      display: flex;
      flex-direction: row;
      padding-left: 220px;
    }

    .detail-info {
      flex-shrink: 0;
      width: 220px;
      height: 100vh;
      overflow-y: auto;
      padding: 2.5rem 2rem;
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      border-right: 1px solid #f0f0f0;
    }

    .detail-images {
      flex: 1;
      min-width: 0;
      display: flex;
      align-items: center;
      gap: 0.5rem;
      overflow-x: auto;
      overflow-y: hidden;
      height: 100vh;
      padding: 0.75rem 2rem;
      -webkit-overflow-scrolling: touch;
    }

    .detail-img {
      height: 100%;
      max-height: 700px;
      width: auto;
      object-fit: contain;
      flex-shrink: 0;
      display: block;
    }
  }

  /* ── Info typography ── */
  .info-title { font-family: 'Lato', sans-serif; font-size: 15px; color: #222; }
  .info-year  { font-family: 'Lato', sans-serif; font-size: 13px; color: #555; }
  .info-medium { font-family: 'Lato', sans-serif; font-size: 13px; color: #999; margin-top: 0.5rem; }
  .info-dims  { font-family: 'Lato', sans-serif; font-size: 13px; color: #bbb; }
  .info-price { font-family: 'Lato', sans-serif; font-size: 13px; font-weight: 700; color: #999; margin-top: 0.5rem; }
  .info-blurb { font-family: 'Lato', sans-serif; font-size: 13px; color: #999; line-height: 1.65; margin-top: 1.25rem; }
  .info-sold  { font-family: 'Lato', sans-serif; font-size: 12px; letter-spacing: 0.1em; text-transform: uppercase; color: #bbb; margin-top: 1.25rem; }
  .buy-btn {
    display: inline-block; align-self: flex-start;
    font-family: 'Lato', sans-serif; font-size: 13px;
    color: #fff; background: #222; text-decoration: none;
    padding: 0.35rem 0.75rem; margin-top: 1.25rem;
    transition: background 0.2s ease;
  }
  .buy-btn:hover { background: #000; }
  .back-link {
    font-family: 'Lato', sans-serif; font-size: 13px;
    color: #bbb; text-decoration: none;
    margin-top: auto; padding-bottom: 2.5rem;
    transition: color 0.2s ease;
  }
  .back-link:hover { color: #222; }
</style>
